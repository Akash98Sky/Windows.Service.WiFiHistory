namespace WiFiHistory.Service.Helpers
{
    public static class CustomConstants
    {        
        public static readonly string NoTimeConfiguredInSettings = "There is no time configured in the appsettings";
        public static readonly string NoDbConnectionStringSettings = "There is no DB connection string configured in the appsettings";
    }
}
